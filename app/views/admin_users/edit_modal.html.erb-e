		<%= form_for(:admin_user, :url => {:action => 'update', :id => @admin_user.id}) do |f| %>
			<div class="modal-body">
				<%= render(:partial => "form", :locals => {:f => f}) %>	
			</div><!-- End body -->
      <div class="modal-footer ">
        <%= button_tag(type: 'submit', class: "btn btn-success btn-lg", style: "width: 100%;", name: "commit") do %>
				 <span class="glyphicon glyphicon-ok-sign"></span>Create
				<% end %>
			</div><!-- End Footer -->
			<% end %>
      